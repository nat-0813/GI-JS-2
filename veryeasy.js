function min(numbers) { //function named min
    return Math.min(numbers); //used the math.min method to return the minimum of the 2 numbers
}
console.log(min(2,3)); //any numbers can be used as the argument